@extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">teachers_material {{ $teachers_material->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/teachers_materials') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/teachers_materials/' . $teachers_material->id . '/edit') }}" title="Edit teachers_material"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['teachers_materials', $teachers_material->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete teachers_material',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $teachers_material->id }}</td>
                                    </tr>
                                    <tr><th> File Name </th><td> {{ $teachers_material->file_name }} </td></tr><tr><th> File Directory </th><td> {{ $teachers_material->file_directory }} </td></tr><tr><th> Teacher Ssn </th><td> {{ $teachers_material->teacher_ssn }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
